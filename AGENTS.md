# 작업 규칙

- 이 저장소는 SAP에서 실행되는 공통 ABAP 구성요소를 관리한다.
- 현재는 초기 구조 단계다. SAP 릴리즈와 공개 API는 확정되지 않았다.
- 기준 구현은 `src/`에 둔다. 실제 소스 도입 시 abapGit 형식을 확정한다.
- 공개 메서드와 타입 변경 시 `docs/public-api.md`와 `docs/migration.md`를 갱신한다.
- 메시지 번호의 기존 의미를 임의로 바꾸지 않는다.
- 업무 검증 결과와 실행 장애의 처리 계약을 명시한다.
- 동작 변경에는 해당 동작을 검증하는 테스트를 포함한다.
- `examples/`에는 최소 사용 예제를 두고 공통 클래스 구현을 복제하지 않는다.
- 실행하지 않은 테스트와 SAP 활성화를 완료했다고 기록하지 않는다.
- 여러 작업자가 함께 작업할 수 있다. 다른 작업자의 변경을 되돌리지 않는다.
- 공개 가능한 코드와 합성 데이터만 커밋한다. 자격증명, 실제 업무 데이터, 로컬 접속 설정은 제외한다.
