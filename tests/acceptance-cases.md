# 인수 조건

상태: 후보 목록. API 확정 후 구체적인 입력과 기대값을 정의합니다.

- 필수값 누락을 식별한다.
- 경고와 오류의 처리 차이가 계약과 일치한다.
- 여러 위반을 정해진 방식으로 반환한다.
- 예외 변환에서 원인 체인을 보존한다.
- 메시지 식별자와 변수 매핑이 계약과 일치한다.
- RAP 호출자와 클래식 호출자의 사용 예제가 동작한다.

테스트 실행 결과: 아직 없음.
